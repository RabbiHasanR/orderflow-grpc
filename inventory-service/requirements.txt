# inventory-service runtime dependencies (what the container image ships).
# Django is used here purely as an ORM + migration system; the runtime server is
# our own gRPC server, not Django's web server.
#
# Stub regeneration (grpcio-tools / protoc) is a DEV-time concern only and lives
# in requirements-dev.txt — we never ship a protobuf compiler in the image.

Django>=5.1,<6.0
psycopg[binary]>=3.2          # PostgreSQL driver (psycopg3); the binary wheel bundles libpq

# gRPC runtime — serving RPCs and the generated stubs depend on these.
# PINNED EXACTLY and kept identical across both services + requirements-dev.txt:
# generated *_pb2_grpc.py embeds the grpcio-tools version and refuses to import
# under an older runtime grpcio, so a floating range lets the two images drift to
# incompatible versions. Regenerate with the matching grpcio-tools==1.81.1.
grpcio==1.81.1
grpcio-health-checking==1.81.1  # grpc.health.v1 servicer + client; pin matches grpcio
protobuf==6.33.6
